"\"\"\"Fabric Warehouse SQLAlchemy Engine Utilities\"\"\"\n+\n+from __future__ import annotations\n+\n+import logging\n+import struct\n+from typing import Optional\n+\n+import pyodbc\n+import sqlalchemy as sa\n+\n+logger = logging.getLogger(__name__)\n+\n+\n+class MockCredentials:\n+    def getToken(self, resource: str) -> str:\n+        logger.warning(\"Using mock credentials for resource: %s\", resource)\n+        return \"FAKE_TOKEN\"\n+\n+\n+def _get_latest_sql_driver() -> str:\n+    drivers = pyodbc.drivers()\n+    sql_drivers = [d for d in drivers if \"ODBC Driver\" in d]\n+    if not sql_drivers:\n+        raise RuntimeError(\"No suitable ODBC driver for SQL Server found.\")\n+    latest = max(sql_drivers)\n+    logger.info(\"Using ODBC driver: %s\", latest)\n+    return latest\n+\n+\n+def get_fabric_warehouse_engine(\n+    sql_endpoint: str,\n+    port: int = 1433,\n+    credentials: Optional[object] = None,\n+) -> sa.engine.Engine:\n+    if not sql_endpoint:\n+        raise ValueError(\"sql_endpoint is required\")\n+    if credentials is None:\n+        credentials = MockCredentials()\n+\n+    driver = _get_latest_sql_driver()\n+    token = credentials.getToken(\"https://database.windows.net/\").encode(\"UTF-16-LE\")\n+    attrs_before = struct.pack(f\"<I{len(token)}s\", len(token), token)\n+\n+    connection_string = f\"DRIVER={{{driver}}};SERVER={sql_endpoint},{port};\"\n+    connection_url = sa.engine.URL.create(\"mssql+pyodbc\", query={\"odbc_connect\": connection_string})\n+\n+    return sa.create_engine(\n+        connection_url,\n+        connect_args={\"attrs_before\": {1256: attrs_before}},\n+        pool_pre_ping=True,\n+        pool_recycle=3600,\n+    )\n*** End Patch**
